# Publications

Each subdirectory is one citable data publication.

```text
publications/<release>/
```

Use adjacent release directories for adjacent products. Do not split one
publication across top-level artifact-family directories.
